{{~#if hasDateType}}
import java.util.Date;
{{~/if}}

public class {{{className}}} {
    {{~#each propertyList}}
    private {{{propertyType}}} {{{name}}};
    {{~/each}}

    public static {{{className}}} of() {
        return new {{{className}}}();
    }

    {{~#each propertyList}}

    public {{{../className}}} {{{setMethodName}}}({{{propertyType}}} {{{name}}}) {
        this.{{{name}}} = {{{name}}};
        return this;
    }

    public {{{../className}}} {{{getMethodName}}}() {
        return this.{{{name}}};
    }
    {{~/each}}
}